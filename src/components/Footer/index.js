import './Footer.scss';
import Social from '../Social';

const Footer = () => {
    return (
        <footer className="text-center">
            <Social/>
        </footer>
    );
};

export default Footer;