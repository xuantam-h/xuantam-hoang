import './Header.scss';
import Navbar from '../Navbar/';

const Header = () => {
    return (
        <header className="main-header padding-global">
            <Navbar/>
        </header>
    );
};

export default Header;